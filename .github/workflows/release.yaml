name: Release

on:
  push:
    branches: [main]

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org
          cache: pnpm

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Verify
        run: pnpm check:build

      - name: Create release PR or publish npm packages
        id: changesets
        uses: changesets/action@v1
        with:
          publish: pnpm release:core
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Detect VSCode extension version change
        id: vscode_changed
        if: steps.changesets.outputs.hasChangesets == 'false'
        shell: bash
        run: |
          if git diff --name-only HEAD^ HEAD | grep -q '^packages/vscode-ext/package.json$'; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

    #   - name: Publish VSCode extension
    #     if: steps.changesets.outputs.hasChangesets == 'false' && steps.vscode_changed.outputs.changed == 'true'
    #     run: pnpm --filter redactor exec vsce publish --pat "$VSCE_PAT"
    #     env:
    #       VSCE_PAT: ${{ secrets.VSCE_PAT }}
