{
  "name": "redact-vscode",
  "version": "0.1.0",
  "main": "dist/extension.js",
  "private": false,
  "engines": {
    "vscode": "^1.90.0"
  },
  "activationEvents": [
    "onCommand:redact.anonymize",
    "onCommand:redact.diff"
  ],
  "contributes": {
    "commands": [
      {
        "command": "redact.anonymize",
        "title": "Anonymize Code"
      },
      {
        "command": "redact.diff",
        "title": "Anonymize Code (Preview Diff)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "redact.anonymize",
          "group": "navigation"
        },
        {
          "command": "redact.diff",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "RedAct",
      "properties": {
        "redAct.enableEmails": {
          "type": "boolean",
          "default": true,
          "description": "Detect and replace email addresses."
        },
        "redAct.enableUrls": {
          "type": "boolean",
          "default": true,
          "description": "Detect and replace URLs."
        },
        "redAct.enableIps": {
          "type": "boolean",
          "default": true,
          "description": "Detect and replace IP addresses."
        },
        "redAct.enableSecrets": {
          "type": "boolean",
          "default": true,
          "description": "Detect and replace secrets."
        },
        "redAct.enableNames": {
          "type": "boolean",
          "default": true,
          "description": "Detect and replace human names."
        },
        "redAct.customPatterns": {
          "type": "array",
          "default": [],
          "description": "Custom regex replacement rules.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "regex": {
                "type": "string"
              },
              "replace": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "regex",
              "replace"
            ]
          }
        },
        "redAct.openInNewTab": {
          "type": "boolean",
          "default": true,
          "description": "Open anonymized output in a new editor tab."
        },
        "redAct.highlight.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable inline highlight of anonymized values."
        },
        "redAct.highlight.color": {
          "type": "string",
          "default": "rgba(0, 255, 0, 0.15)",
          "description": "Background color for anonymized highlights."
        }
      }
    }
  },
  "scripts": {
    "build": "tsup src/extension.ts --format cjs --external vscode",
    "dev": "tsup src/extension.ts --watch --format cjs --external vscode",
    "typecheck": "pnpm --filter @code-anonymizer/core build && tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@code-anonymizer/core": "workspace:*"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  },
  "displayName": "RedAct",
  "description": "RedAct: anonymize sensitive code snippets before sharing."
}
